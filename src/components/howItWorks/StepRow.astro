---
/**
 * Alternating text/visual row. `reverse` flips sides at `lg`.
 * Content goes in the default slot, the visual in slot="visual".
 */
const { reverse = false, eyebrow, class: className = "" } = Astro.props;
---

<div
  class:list={[
    "flex flex-col items-center gap-10 rounded-[20px] bg-white px-3 pb-3 pt-5 shadow-[0_0_1px_rgba(0,0,0,0.25)]",
    "lg:grid lg:grid-cols-2 lg:items-center lg:gap-16 lg:rounded-none lg:bg-transparent lg:px-0 lg:py-0 lg:shadow-none",
    className,
  ]}
>
  <div
    class:list={[
      "flex flex-col items-center gap-4 text-center lg:items-start lg:text-left",
      reverse && "lg:order-2",
    ]}
  >
    {eyebrow && (
      <div class="flex items-center gap-2">
        <span class="text-xs font-bold tracking-wide text-indigo-500">
          {eyebrow}
        </span>
        <span class="h-px w-3.5 bg-indigo-500 lg:w-8 lg:bg-gray-200" aria-hidden="true"></span>
      </div>
    )}
    <slot />
  </div>

  <div class:list={["flex w-full items-center justify-center", reverse && "lg:order-1"]}>
    <slot name="visual" />
  </div>
</div>